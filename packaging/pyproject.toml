[project]
name = "one-deploy"
version = "0.0.0"
description = "OpenNebula Ansible Playbooks"
authors = ["OpenNebula <contact@opennebula.io>"]
license = "Apache-2.0"
readme = "README.md"
requires-python = ">=3.9"
dependencies = [
  "ansible-core<2.17",
  "netaddr",
]
